{% extends '_base.html.twig' %}

{% block title %}Liste Utilisateur{% endblock %}

{% block body %}
    <link rel="stylesheet" href="{{ asset('asset/css/registerr.css') }}" />
    <link rel="stylesheet" href="{{ asset('asset/css/comunee.css') }}" />

    <div class="container">
        <div class="page-inner pt-3">
            <div class="page-header m-0">
                <ul class="breadcrumbs mb-3">
                    <li class="nav-home">
                        <a href="{{ path('app_users') }}">
                            <i class="icon-home"></i>
                        </a>
                    </li>
                    <li class="separator">
                        <i class="icon-arrow-right"></i>
                    </li>
                    <li class="nav-item">
                        <a href="#">Utilisateurs</a>
                    </li>
                    <li class="separator">
                        <i class="icon-arrow-right"></i>
                    </li>
                    <li class="nav-item">
                        <a href="#">Liste Utilisateurs</a>
                    </li>
                </ul>
            </div>
            <div class="row">
                <div class="col-md-12">
                    <div class="card">
                        <div class="card-header">
                            <div class="d-flex align-items-center">
                                <h4 class="card-title">Utilisateurs <span class="badge bg-soft-dark text-dark ms-2">{{users|length}}</span></h4>
                                <button class="btn btn-primary btn-round ms-auto" data-bs-toggle="modal" data-bs-target="#registerModal">
                                    <i class="fa fa-plus"></i>
                                    Nouveau
                                </button>
                            </div>
                        </div>
                        <div class="card-body">
                            <form method="GET" action="{{ path('app_users') }}">
                                <div class="row d-flex justify-content-between">
                                    <!-- Items per Page Dropdown -->
                                    <div class="row col-md-6 d-flex">
                                        <div class="col-md-2">
                                            <select name="itemsPerPage" id="itemsPerPage" class="form-control mb-2" onchange="this.form.submit()">
                                                <option value="10" {% if itemsPerPage == 10 %}selected{% endif %}>10</option>
                                                <option value="25" {% if itemsPerPage == 25 %}selected{% endif %}>25</option>
                                                <option value="50" {% if itemsPerPage == 50 %}selected{% endif %}>50</option>
                                                <option value="100" {% if itemsPerPage == 100 %}selected{% endif %}>100</option>
                                            </select>
                                        </div>
                                        <div class="col-md-4">
                                            <select name="userType" id="userType" class="form-control mb-2" onchange="this.form.submit()">
                                                <option value="">Tous les utilisateurs</option>
                                                <option value="ROLE_ADMIN" {% if userType == 'ROLE_ADMIN' %}selected{% endif %}>Administrateur</option>
                                                <option value="ROLE_USER" {% if userType == 'ROLE_USER' %}selected{% endif %}>Utilisateur</option>
                                            </select>
                                        </div>
                                    </div>

                                    <!-- Search Input -->
                                    <div class="col-md-6">
                                        <input type="text" name="search" id="dossierSearch" class="form-control"
                                               placeholder="Rechercher un dossier..." value="{{ searchQuery }}">
                                    </div>

                                    <!-- Hidden Submit Button (Not necessary but ensures form submission) -->
                                    <button type="submit" hidden></button>
                                </div>
                            </form>
                            <div class="table-responsive">
                                {% if users is not empty %}
                                <table class="table table-hover table-bordered">
                                    <thead>
                                    <tr class="head">
                                        <th style="text-transform: capitalize;">Username</th>
                                        <th style="text-transform: capitalize;">Email</th>
                                        <th style="text-transform: capitalize;">Role</th>
                                        <th style="width: 10%;text-transform: capitalize;">Action</th>
                                    </tr>
                                    </thead>
                                    <tbody>
                                    {% for user in users %}
                                        <tr>
                                            <td>{{ user.username }}</td>
                                            <td>{{ user.email }}</td>
                                            {% set role = user.getRoles()[0] %}
                                            {% if role == 'ROLE_ADMIN' %}
                                                <td>Administrateur</td>
                                            {% else %}
                                                <td>Utilisateur</td>
                                            {% endif %}

                                            <td>
                                                <div class="form-button-action">
                                                    <a class="btn btn-icon" href="#">
                                                        <i class="fas fa-eye fa-sm text-dark"></i>
                                                    </a>
                                                    <a class="btn btn-icon mx-2" href="#">
                                                        <i class="fas fa-edit fa-sm text-success"></i>
                                                    </a>
                                                    <form id="delete-client-form-{{ user.id }}" action="#" method="POST">
                                                        <button type="button" class="btn btn-icon delete-client-btn" data-client-id="{{ user.id }}">
                                                            <i class="fas fa-trash-alt fa-sm text-danger"></i>
                                                        </button>
                                                    </form>
                                                </div>
                                            </td>
                                        </tr>
                                    {% endfor %}

                                    </tbody>
                                </table>
                                {% else %}
                                <div class="alert-search alert_warning text-center">Aucun résultat trouvé.</div>
                                {% endif %}
                            </div>
                            <div class="row navigation d-flex justify-content-center mt-3" >
                                {{ knp_pagination_render(pagination,'@KnpPaginator/Pagination/twitter_bootstrap_v4_pagination.html.twig') }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- Modal Structure -->
    <div class="modal fade" id="registerModal" tabindex="-1" aria-labelledby="registerModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content" style="border-radius: 25px;box-shadow: 2px 6px 15px 0 rgba(69, 65, 78, .1);border: 0;">
                <div class="modal-body m-0">
                    <div class="container h-100">
                        <div class="row d-flex justify-content-center align-items-center h-100">
                            <div class="col-lg-12">
                                <div class="card text-black mb-0 shadow-none">
                                    <div class="d-flex justify-content-end align-items-center">
                                        <button type="button" class="btn-close text-right" data-bs-dismiss="modal" aria-label="Close"></button>
                                    </div>
                                    <div class="card-body p-0">
                                        <div class="row justify-content-center">
                                            <div class="col-md-12 col-lg-12 col-xl-12 order-2 order-lg-1">

                                                <p class="text-center h1 mb-5 mx-1 mx-md-4 mt-4 text-muted" style="font-weight: 900">Sign up</p>

                                                {{ form_errors(registrationForm) }}

                                                {{ form_start(registrationForm, { attr: { class: 'mx-1 mx-md-4' } }) }}

                                                <div class="d-flex flex-row align-items-center mb-4">
                                                    <i class="fas fa-user fa-lg me-3 fa-fw text-muted"></i>
                                                    <div data-mdb-input-init class="form-outline flex-fill mb-0">
                                                        {{ form_widget(registrationForm.username) }}
                                                        {{ form_label(registrationForm.username, 'Your Name') }}
                                                        <div class="form-notch">
                                                            <div class="form-notch-leading"></div>
                                                            <div class="form-notch-middle"></div>
                                                            <div class="form-notch-trailing"></div>
                                                        </div>
                                                    </div>
                                                </div>

                                                <div class="d-flex flex-row align-items-center mb-4">
                                                    <i class="fas fa-envelope fa-lg me-3 fa-fw text-muted"></i>
                                                    <div data-mdb-input-init class="form-outline flex-fill mb-0">
                                                        {{ form_widget(registrationForm.email) }}
                                                        {{ form_label(registrationForm.email, 'Your Email') }}
                                                        <div class="form-notch">
                                                            <div class="form-notch-leading"></div>
                                                            <div class="form-notch-middle"></div>
                                                            <div class="form-notch-trailing"></div>
                                                        </div>
                                                    </div>
                                                </div>

                                                <div class="d-flex flex-row align-items-center mb-4">
                                                    <i class="fas fa-lock fa-lg me-3 fa-fw text-muted"></i>
                                                    <div data-mdb-input-init class="form-outline flex-fill mb-0">
                                                        {{ form_widget(registrationForm.plainPassword) }}
                                                        {{ form_label(registrationForm.plainPassword, 'Password') }}
                                                        <div class="form-notch">
                                                            <div class="form-notch-leading"></div>
                                                            <div class="form-notch-middle"></div>
                                                            <div class="form-notch-trailing"></div>
                                                        </div>
                                                    </div>
                                                </div>

                                                <div class="d-flex flex-row align-items-center mb-3">
                                                    <i class="fas fa-key fa-lg me-3 fa-fw text-muted"></i>
                                                    <div data-mdb-input-init class="form-outline flex-fill mb-0">
                                                        <input type="password" id="registration_form_repeatPassword" class="form-control" />
                                                        <label class="form-label" for="registration_form_repeatPassword">Repeat your password</label>
                                                        <div class="form-notch">
                                                            <div class="form-notch-leading"></div>
                                                            <div class="form-notch-middle"></div>
                                                            <div class="form-notch-trailing"></div>
                                                        </div>
                                                    </div>
                                                </div>

                                                <div class="d-flex flex-row align-items-center mb-3">
                                                    {{ form_widget(registrationForm.image) }}

                                                </div>

                                                <div class="error-message" id="error-message">Passwords do not match!</div> <!-- Error message placeholder -->

                                                <div class="d-flex flex-row justify-content-between align-items-center mb-4">
                                                    <label for="userType" class="form-label mb-0" style="margin-left: 35px">User type</label>
                                                    <select name="userType" id="userType" class="form-select" style="flex: 1; max-width: 200px;">
                                                        <option value="ROLE_ADMIN">Admin</option>
                                                        <option value="ROLE_USER">User</option>
                                                    </select>
                                                </div>

                                                <div class="form-check d-flex justify-content-center mb-4">
                                                    <input type="checkbox"
                                                           id="{{ registrationForm.agreeTerms.vars.id }}"
                                                           name="{{ field_name(registrationForm.agreeTerms) }}"
                                                           class="form-check-input me-2"
                                                           value="{{ field_value(registrationForm.agreeTerms) }}"
                                                            {% if registrationForm.agreeTerms.vars.required %} required="required" {% endif %}
                                                    >
                                                    <label class="form-check-label" for="registration_form_agreeTerms">
                                                        I agree all statements in <a href="#!">Terms of service</a>
                                                    </label>
                                                </div>

                                                <div class="d-flex justify-content-center mx-4 mb-3 mb-lg-4">
                                                    <button type="submit" class="btn btn-primary btn-lg">
                                                        Register
                                                    </button>
                                                </div>

                                                {{ form_end(registrationForm) }}

                                            </div>
{#                                            <div class="col-md-10 col-lg-6 col-xl-6 d-flex align-items-center order-1 order-lg-2">#}
{#                                                <img src="https://mdbcdn.b-cdn.net/img/Photos/new-templates/bootstrap-registration/draw1.webp" class="img-fluid" alt="Sample image">#}
{#                                            </div>#}
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="{{ asset('asset/js/core/jquery-3.7.1.min.js') }}"></script>
    <script>

        document.addEventListener("DOMContentLoaded", function () {
            // Select all input fields with the class "form-control"
            const inputs = document.querySelectorAll(".form-control");

            inputs.forEach(input => {
                // Add event listener for "input" event
                input.addEventListener("input", function () {
                    if (this.value.trim() !== "") {
                        this.classList.add("active");
                    } else {
                        this.classList.remove("active");
                    }
                });
            });
        });

        $(document).ready(function () {
            $(".delete-client-btn").click(function (e) {
                e.preventDefault(); // Prevent the default button action
                const clientId = $(this).data("client-id");
                const form = $("#delete-client-form-" + clientId); // Get the form associated with this button

                swal({
                    title: "Are you sure?",
                    text: "You won't be able to revert this!",
                    icon: "warning",
                    buttons: {
                        confirm: {
                            text: "Yes, delete it!",
                            className: "btn btn-danger",
                        },
                        cancel: {
                            visible: true,
                            className: "btn btn-success",
                        },
                    },
                }).then((Delete) => {
                    if (Delete) {
                        form.submit(); // Submit the form if confirmed
                    } else {
                        swal.close();
                    }
                });
            });
        });

    </script>

{% endblock %}